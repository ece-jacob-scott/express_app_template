<header class="mui-appbar mui--z1">
  <div class="mui-container">
    <table>
      <tr class="mui--appbar-height">
        <td class="mui--text-title">
            <a href="/" style="text-decoration: none;">Brand.io</a>
        </td>
        <td class="mui--text-right">
          <ul class="mui-list--inline mui--text-body2">
            <!-- <li><a href="#">About</a></li>
            <li><a href="#">Pricing</a></li> -->
            <% if (user) { %>
            <li>
              <a onclick="handleLogout('/logout')" style="cursor: pointer;">
                <i class="fa fa-user" aria-hidden="true"></i> Logout</a>
              </form>
            </li>
            <% } %> <% if (!user) { %>
            <li>
              <a href="/login"
                ><i class="fa fa-user" aria-hidden="true"></i> Login</a
              >
            </li>
            <li>
              <a href="/register">
                <i class="fa fa-user" aria-hidden="true"></i> Register</a
              >
            </li>
            <% } %>
          </ul>
        </td>
      </tr>
    </table>
  </div>
</header>

<script type="text/javascript">
// this is used to logout the user
function handleLogout(url) {
    const postForm = document.createElement('form');
    postForm.action = url;
    postForm.method = 'post';
    const bodyTag = document.getElementsByTagName('body')[0];
    bodyTag.appendChild(postForm);
    postForm.submit();
}
</script>